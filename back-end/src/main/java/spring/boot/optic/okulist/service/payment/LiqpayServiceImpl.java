package spring.boot.optic.okulist.service.payment;

import org.springframework.stereotype.Service;

@Service
public class LiqpayServiceImpl {

  /*  @Override
    public OrderResponseDto addOrder(Long id, CreateOrderRequestDto createOrderRequestDto) {
        // ... (логіка створення замовлення)

        // Ініціювати платіж Liqpay
        try {
            LiqpayPaymentResponse paymentResponse = liqpayService.initiatePayment(order.getId(),
             order.getTotal().toString());
            // Перенаправте користувача на сторінку оплати Liqpay, використовуючи paymentResponse.data
        } catch (Exception e) {
            // Обробка помилок ініціювання платежу
        }

        return orderMapper.toDto(order);
    }

   */
}
