import React from 'react';
import Catalog from '../../components/Catalog/Catalog';

const CatalogPage = () => {
    return (
        <main>
            <Catalog />
        </main>
    );
};

export default CatalogPage;
