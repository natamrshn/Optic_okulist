import React from 'react';

const Login = () => {
    return (
        <>
            <input className='modal-input' type='email' placeholder='Enter your email' />
            <input className='modal-input' type="password" placeholder='Password' />
        </>
    );
};

export default Login;
